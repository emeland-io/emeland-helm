apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emeland.fullname" . }}-test-connection"
  labels:
    {{- include "emeland.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emeland.fullname" . }}-server:{{ .Values.service.port }}']
  restartPolicy: Never
